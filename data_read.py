import pandas as pd 
import numpy as np 
import os
import glob
import json

# def _init_ (self, path) :
#     self.path = path
#     self.match = self.match_data()
#     (self.player, self.player_match) = self.player_data()
#     (self.delivery, self.extras, self.wickets, self.fielder_wickets) = self.delivery_data()
#     self.player_match_data # uses delivery data to auto-populate some basic scorecard stats in player_match

